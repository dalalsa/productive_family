

<%if seller_signed_in?%>
  <%= render partial: 'seller_show' %>
  <% @seller.products.each do |p| %>
    <%= p.name %>
    <%= link_to 'Delete', product_path(p), method: :delete, data: { confirm: 'Are you sure?' } %>
    <%= link_to 'Edit', edit_product_path(p)%>
<%end %>
<% else %>
  <h3><%= @seller.name %> <span><%= rating_for @seller, "quality", stars: 10 %> </span> </h3>
    <%#= imdb_style_rating_for @seller, current_user%> 


  <h5>Our products:   </h5> 
  <div class="row">
  <% @seller.products.each do |product| %>
  <div class="col s8">
      <%= product.name %>
      <p> Price: <%= product.price %> S.R</p>
      <%#=  cl_image_tag(product.image, :width => 70, :height => 70) %> 
      <%#= console %>

      <div><%= button_to "Add #{product.name} to cart", items_path(:product_id => product.id) %></div>
        <%#= link_to "Add one", item_add_path(:item_id => item.id), method: :post %>
        <%#= link_to "Reduce one", item_reduce_path(:id => item), method: :post %>
      </div>
  <% end %>
  <% end %>
  </div>
  </div>


<!-- orders history-->

    
 
    <%# if current_user %>
      quality: 
      
    <%## end %>

 